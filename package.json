{
  "name": "aoe-web",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -p tsconfig.build.json && vite build",
    "preview": "vite preview",
    "generate-atlas": "node scripts/generate-unit-atlas.mjs",
    "test": "vitest",
    "test:run": "vitest run",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^4.1.0",
    "happy-dom": "^20.8.4",
    "typescript": "^5.4.0",
    "vite": "^5.2.0",
    "vitest": "^4.1.0"
  }
}
